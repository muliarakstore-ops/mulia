"# Supabase Integration Walkthrough - Mulia Rak Store\n\nThis walkthrough documents the end-to-end integration of the Supabase backend with Mulia Rak Store's website landing page and administrative dashboard.\n\n## Completed Implementations\n\n### 1. Database Schema (`supabase_setup.sql`)\nCreated a comprehensive relational schema in Supabase containing:\n* **Singleton Tables**: `cms_pages`, `profile`, and `about_us` configured with primary key check constraints to restrict each table to exactly one configurations row (`00000000-0000-0000-0000-000000000000`).\n* **Catalog Data**: `products` (containing categories, dimensions, and stock counters) and `services` (containing service icon uploads).\n* **Telemetry & Inquiries**: `analytics_metrics` and `leads` for logging visitors, screentimes, and direct WhatsApp inquiries.\n* **Finances**: `transactions` (for accounting log entries), `restock_history`, and `restock_items`.\n* **Automatic Inventory Update Trigger**: Added `trg_restock_stock_update` trigger on the `restock_items` table. It automatically increments `products.stock` whenever new inventory is added.\n\n### 2. Row Level Security (RLS) Policies\n* Configured anonymous client `anon` policies to grant select-only permissions for CMS texts, profile records, and catalog pages.\n* Allowed anonymous clients to submit inquiries (`INSERT` on `leads` and `analytics_metrics`).\n* Locked write permissions and ledger access exclusively to `authenticated` users (admins).\n\n### 3. Data Integration Layer (`src/utils/supabaseData.ts`)\n* Added `isSupabaseConfigured` helper checking environmental settings.\n* Added CRUD methods for Products, Services, Analytics, and CMS configurations.\n* Provided automatic fallbacks to local storage if Supabase credentials are not configured in the host environment.\n\n### 4. Admin Dashboard Updates (`src/app/admin/page.tsx`)\n* Configured the mount hook `useEffect` to trigger parallelized asynchronous fetches for all configuration and finance logs from Supabase.\n* Swapped out previous 
<truncated 492 bytes>